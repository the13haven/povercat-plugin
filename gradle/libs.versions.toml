[versions]

tomlj = "1.1.1"

axion-release-plugin = "1.19.1"

junit = "5.13.1"
junit-platform = "1.13.1"
mockk = "1.14.2"

jacoco = "0.8.13"
testkit-jacoco-plugin = "1.0.3"
gradle-plugin-publish = "1.3.1"

[libraries]

tomlj = { group = "org.tomlj", name = "tomlj", version.ref = "tomlj" }

jacoco = { group = "org.jacoco", name = "org.jacoco.core", version.ref = "jacoco" }

junit-bom = { group = "org.junit", name = "junit-bom", version.ref = "junit" }
junit-jupiter = { group = "org.junit.jupiter", name = "junit-jupiter", version.ref = "junit" }
junit-platform-launcher = { group = "org.junit.platform", name = "junit-platform-launcher", version.ref = "junit-platform" }

mockk = { group = "io.mockk", name = "mockk", version.ref = "mockk" }

[bundles]

[plugins]

axion-release-plugin = { id = "pl.allegro.tech.build.axion-release", version.ref = "axion-release-plugin" }

testkit-jacoco-plugin = { id = "io.github.gmazzo.gradle.testkit.jacoco", version.ref = "testkit-jacoco-plugin" }

gradle-plugin-publish = { id = "com.gradle.plugin-publish", version.ref = "gradle-plugin-publish" }
